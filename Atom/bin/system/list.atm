(Size call) (size) let
(Item call) (item) let
(Join call) (join) let
((split.list) let (split.list) load size (split.size) let ((split.list) load split.size counter - item) split.size loop drop drop) (split) let
(load 0 item)(load-head 'load head of the list associated with the tos') let